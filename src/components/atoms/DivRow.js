import styled from "styled-components";
import DivContainer from "./DivContainer";

const DivRow = styled(DivContainer)`
    flex-direction: row;
    justify-content: center;
    align-items: center;
    height: 100%;
`;

export default DivRow;
